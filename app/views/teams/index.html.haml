%table.m-table
  %thead.m-table--head
    %tr.m-table--row
      %td
      %td.m-user-list
      %td Spieler
      %td Quote
      %td Spiele
      %td{title: "Quote x Log10(Anzahl der Spiele)"} Punkte

  %tbody.m-table--body
    - @teams.each_with_index do |team, i|
      %tr.m-table--row
        %td= i+1
        %td.m-user-list
          .m-user.as-selected{title: team.users.first.name}= user_image(team.users.first)
          .m-user.as-selected{title: team.users.last.name}= user_image(team.users.last) if team.double?
        %td= team.name
        %td= "#{team.percentage}%"
        %td= team.number_of_games
        %td{title: "#{team.percentage} x Log10(#{team.number_of_games})"}= team.value
